<%= render 'shared/header', header: "Create a Status" %>
<%= render 'shared/errors', obj: @status %>


 <%= form_for @status do |f| %>
   <%= f.label :body %>
   <%= f.text_area :body %>
   <br/>
   <%= f.submit "Create Status", class: "btn btn-dafault" %>
 <% end %>
